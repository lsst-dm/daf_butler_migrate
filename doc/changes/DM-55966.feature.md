Added support for out-of-band migrations, e.g. applying patches to the schema that do not change versions in `butler_attributes`.
This is implemented as a new alembic revision tree(s), the name of the tree with the patches has to have a suffix `-patches`.
The prefix of the tree name is supposed to match the name of the corresponding tree (e.g. `dimensions-config`), but that is just a convention.
